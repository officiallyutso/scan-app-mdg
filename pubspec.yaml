name: scan
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.0-334.4.beta

dependencies:
  flutter:
    sdk: flutter
  image: ^4.0.17  # For image cropping and roatation
  camera: ^0.10.5+9
  path_provider: ^2.1.2
  path: ^1.8.3
  http: ^1.2.0
  http_parser: ^4.0.2
  image_picker: ^1.0.7
  google_fonts: ^6.1.0
  flutter_svg: ^2.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  ## future use
  assets:
    - assets/icons/
